R"(这是一个原始字符串字面量)"
R"delimiter(这是带有自定义分隔符的原始字符串)delimiter"
// 基础字符串测试
"这是一个普通字符串"
"这是带有转义字符的字符串\n\t\r\"\\'"
'这是一个字符'
"这是一个包含Unicode的字符串：你好，世界！"
R"(这是一个
多行的
原始字符串
字面量)"
"这是一个\
续行的\
字符串"
'\\' // 转义的反斜杠字符
'\n' // 换行字符
'\t' // 制表符
'\"' // 引号字符
R"test(原始字符串可以包含任何字符，包括"引号"和\反斜杠\)test"
"混合使用转义序列：\n第一行\n\t缩进的第二行\n\"引用的文本\""
"这是一个空字符串"
""
''

// 转义序列测试
"包含转义字符：\n\t\r\"\\\'\a\b\f\v"
'\n'
'\t'
'\r'
'\''
'\"'
'\\'
"\x41\x42\x43"  // 十六进制转义
"\101\102\103"  // 八进制转义

// Unicode字符测试
"Unicode测试：\u4F60\u597D"
"表情符号：😀 😎 🤔 💡 🌈 🎉"
"中文标点符号：。，！？；：""''（）【】"

// 原始字符串测试
R"(原始字符串测试)"
R"delimiter(带有特殊分隔符的原始字符串)delimiter"
R"test(可以包含任何特殊字符 \n \t " ' \ { } [ ])test"

// 多行字符串测试
R"(
    可以包含任意缩进
        和空格
            以及特殊字符 !@#$%^&*()
第一行
  第二行（带缩进）
    第三行（更多缩进）
/* 可以包含注释符号 */
// 可以包含注释符号
)"
"Unicode字符串示例：🌟 🎈 🎉 ⭐️ 🌈"

// 混合字符串测试
"混合中英文：Hello，世界！"
"特殊字符混合：!@#$%^&*()_+-=[]{}|;:,.<>?"
R"(程序代码示例：
int main() {
    printf("Hello, World!\n");
    return 0;
})"

// 字符串续行测试
"这是一个很长的字符串，\
它被分成了多行，\
但实际上是一行"

// 特殊情况测试
"\0"  // 空字符
"\x00\xFF"  // 最小和最大字节
"混合所有情况：\n\t\"Hello\\\n世界\x41\u4F60\u597D"

// 编程相关字符串
"文件路径测试：C:\\Program Files\\测试程序\\test.exe"
"URL测试：https://www.example.com/测试?param=value"
"JSON示例：{\"name\":\"测试\",\"value\":123}"
"SQL查询：SELECT * FROM users WHERE name='测试'"

// 数学和科学记号
"数学公式：E = mc² ∑∏√∫≠≈"
"化学公式：H₂O CO₂"
"单位：m/s² km/h °C"

// 多语言混合
"多语言混合：English中文日本語한국어العربية"
R"(
/* 多语言代码注释示例 */
// 中文注释
// English Comment
// 日本語コメント
// 한국어 댓글
)"

// 边界测试
"    前面有空格的字符串"
"后面有空格的字符串    "
"中间    有多个空格的字符串"
"\r\n\r\n多个换行符\r\n\r\n"

// 特殊用途字符串
"BASE64示例：SGVsbG8gV29ybGQ="
"正则表达式：^[A-Za-z0-9]+$"
"HTML代码：<div class=\"test\">测试</div>"
"命令行参数：--config=\"test.conf\" -v --help"
